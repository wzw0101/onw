name: Build and Deploy

on:
  push:
    branches: [main]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Login to ACR
        run: echo "${{ secrets.ACR_PASSWORD }}" | docker login ${{ secrets.ACR_REGISTRY }} -u "${{ secrets.ACR_USERNAME }}" --password-stdin

      - name: Build images
        run: docker compose build

      - name: Tag and push backend
        run: |
          docker tag onw-backend:latest ${{ secrets.ACR_REGISTRY }}/onw-backend:latest
          docker push ${{ secrets.ACR_REGISTRY }}/onw-backend:latest

      - name: Tag and push frontend
        run: |
          docker tag onw-frontend:latest ${{ secrets.ACR_REGISTRY }}/onw-frontend:latest
          docker push ${{ secrets.ACR_REGISTRY }}/onw-frontend:latest

      - name: Deploy to server
        uses: appleboy/ssh-action@v1
        with:
          host: ${{ secrets.SERVER_IP }}
          username: root
          key: ${{ secrets.SSH_KEY }}
          script: |
            cd /opt/onw
            docker compose -f docker-compose.remote.yml pull
            docker compose -f docker-compose.remote.yml up -d
            docker image prune -f
